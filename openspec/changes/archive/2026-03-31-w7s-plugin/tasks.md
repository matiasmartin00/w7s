# Tasks: w7s OpenCode Workflows Plugin

## Phase 0: Project Scaffold

- [x] T-01 **Create project scaffold** — `package.json` (name: w7s, type: module, deps: zod, yaml, @opencode-ai/plugin; devDeps: vitest, typescript), `tsconfig.json` (strict, ESM, Bun target), `vitest.config.ts`, `src/index.ts` (export empty plugin fn returning `{}` hooks). Tests: verify plugin fn is callable and returns object. Files: `package.json`, `tsconfig.json`, `vitest.config.ts`, `src/index.ts`. Reqs: none (infrastructure). Size: S

## Phase 1: Foundation — Types, Schemas, Expression Engine

- [x] T-02 **Create types** — `src/types/workflow.ts` (Workflow, Step union, AiPromptStep, ShellStep, ApprovalStep, WorkflowInput, Trigger), `src/types/execution.ts` (ExecutionContext, StepResult, WorkflowResult, ExecutionStatus, StepStatus), `src/types/expression.ts` (ASTNode union: AccessNode, OrNode, ComparisonNode, LiteralNode), `src/types/index.ts` (re-exports). No tests (type-only). Files: 4 files in `src/types/`. Reqs: all (foundational). Size: S
- [x] T-03 **Create Zod schemas** — `src/schema/workflow.ts` (WorkflowSchema, StepSchema discriminated union on type, InputSchema, TriggerSchema), `src/schema/index.ts`. Tests: valid workflow passes, missing `steps` rejected, missing step `id`/`type` rejected, unknown step type rejected, all 3 step types validate correctly, optional fields absent OK. Files: `src/schema/workflow.ts`, `src/schema/index.ts`, `tests/schema/workflow.test.ts`. Reqs: REQ-LOAD-002. Size: S
- [x] T-04 **Expression engine — tokenizer** — `src/expression/tokenizer.ts`: tokenize `${{ expr }}` content into token stream (IDENT, DOT, OR, EQ, NEQ, STRING, BOOL, NUMBER, EOF). Tests: simple ident, dotted path, `||`, `==`/`!=`, string literals, bool literals, whitespace handling, empty input. Files: `src/expression/tokenizer.ts`, `tests/expression/tokenizer.test.ts`. Reqs: REQ-EXPR-001. Size: S
- [x] T-05 **Expression engine — parser + evaluator** — `src/expression/parser.ts` (recursive descent: parseExpression → parseOr → parseComparison → parseAccess → parsePrimary, produces AST), `src/expression/evaluator.ts` (`evaluate(ast, ctx)` resolves vars and applies ops; `interpolate(template, ctx)` finds all `${{ }}` blocks and evaluates). Deps: T-04, T-02. Tests: simple var resolution, nested field `steps.X.output.field`, `||` fallback (first truthy, first falsy), `==`/`!=` comparisons, bool coercion for `when`, unresolved var → empty string, multiple `${{ }}` in one string, no expressions passthrough, `env.VAR` resolution. Files: `src/expression/parser.ts`, `src/expression/evaluator.ts`, `tests/expression/parser.test.ts`, `tests/expression/evaluator.test.ts`. Reqs: REQ-EXPR-001 through REQ-EXPR-005. Size: M

## Phase 2: Loader

- [x] T-06 **Workflow loader** — `src/loader/resolver.ts` (`resolveWorkflowPaths(projectDir)` returns ordered YAML file paths from local + global dirs), `src/loader/parser.ts` (`parseWorkflow(yamlContent, filePath)` YAML parse + Zod validate), `src/loader/registry.ts` (`WorkflowRegistry` class: stores workflows, maps trigger→workflow, `findByTrigger()`, detects duplicate triggers, applies local-over-global precedence). Deps: T-02, T-03. Tests: local + global both loaded, empty dirs → zero workflows no error, invalid YAML rejected with descriptive error (remaining valid ones still load), local overrides global same name, duplicate triggers between workflows → neither registered + error reported, valid workflow parses and registers correctly. Files: `src/loader/resolver.ts`, `src/loader/parser.ts`, `src/loader/registry.ts`, `tests/loader/loader.test.ts`, `tests/fixtures/` (valid + invalid YAML fixtures). Reqs: REQ-LOAD-001 through REQ-LOAD-004. Size: M

## Phase 3: Step Executors + Utilities

- [x] T-07 **Utility modules** — `src/utils/retry.ts` (`withRetry(fn, maxRetries)` with exponential backoff 1s·2^attempt, capped 30s), `src/utils/input-parser.ts` (`parseInputArgs(raw, inputDefs)` parses `key=value` and `--key value`, validates required/defaults), `src/steps/json-extractor.ts` (multi-strategy JSON extraction: regex code fences → bracket-scan → raw parse). Deps: T-02. Tests: retry succeeds on Nth attempt, retry exhausted throws, backoff timing verified; input parser `key=value`, `--key value`, mixed, missing required error, default applied; JSON extractor fenced code, bracket-scan, raw, invalid JSON throws, mixed prose+JSON. Files: `src/utils/retry.ts`, `src/utils/input-parser.ts`, `src/steps/json-extractor.ts`, `tests/utils/retry.test.ts`, `tests/utils/input-parser.test.ts`, `tests/steps/json-extractor.test.ts`. Reqs: REQ-STEP-SHELL-003, REQ-INPUT-001, REQ-INPUT-002, REQ-STEP-AI-002. Size: M
- [x] T-08 **Shell executor** — `src/steps/shell.ts`: `ShellExecutor.execute(step, context, $)` builds command from interpolated `run`, sets env/cwd, runs with `.nothrow().quiet()`, captures stdout/stderr/exitCode, returns `StepResult`. `src/steps/types.ts`: `StepExecutor` interface, `StepResult` type (if not already in types). Deps: T-02, T-05, T-07. Tests: successful command → output + exit 0, failed command → exit != 0 + stderr captured, custom env vars injected, interpolation in `run` string works. Files: `src/steps/shell.ts`, `src/steps/types.ts`, `tests/steps/shell.test.ts`. Reqs: REQ-STEP-SHELL-001, REQ-STEP-SHELL-002. Size: S
- [x] T-09 **AI prompt executor** — `src/steps/ai-prompt.ts`: `AiPromptExecutor.execute(step, context, client)` creates isolated session, sends interpolated prompt to specified agent (or default), captures response, handles `output_format: json` (extract + parse via json-extractor, fail if invalid). Deps: T-02, T-05, T-07. Tests (mock client): session created per step (isolation), prompt sent to specified agent, prompt sent to default when no agent, text output captured, JSON output parsed and fields accessible, invalid JSON → step failure. Files: `src/steps/ai-prompt.ts`, `tests/steps/ai-prompt.test.ts`. Reqs: REQ-STEP-AI-001, REQ-STEP-AI-002, REQ-STEP-AI-003. Size: M
- [x] T-10 **Approval executor** — `src/steps/approval.ts`: `ApprovalExecutor.execute(step, context, client, sessionID)` sends assistant message with `[Approval]` prefix to parent session, polls `session.messages` for user yes/no reply, returns continue or cancel. Deps: T-02, T-05. Tests (mock client): message sent with interpolated content, user confirms → continues, user cancels → returns cancelled status, timeout/edge cases. Files: `src/steps/approval.ts`, `tests/steps/approval.test.ts`. Reqs: REQ-STEP-APPROVAL-001. Size: S

## Phase 4: Execution Engine

- [x] T-11 **Execution engine + context** — `src/engine/context.ts` (`ExecutionContext` class: stores inputs, step outputs, workflow metadata; `get(path)` resolves dotted paths; `set(stepId, result)` stores output). `src/engine/executor.ts` (`ExecutionEngine.run(workflow, inputs, sessionID)` main loop: iterate steps in order, evaluate `when` via expression engine, dispatch to correct executor by type, handle retry via `withRetry`, store outputs in context, on failure → fail-stop). Deps: T-02, T-05, T-07, T-08, T-09, T-10. Tests (mock executors): steps execute in declaration order, `when: false` → step skipped (no output), `when: true` → step runs, step output stored and accessible to next step, failed step stops workflow (subsequent steps don't run), completed steps preserved after failure, retry wraps executor call, cancel from approval stops workflow. Files: `src/engine/context.ts`, `src/engine/executor.ts`, `tests/engine/engine.test.ts`. Reqs: REQ-ENGINE-001 through REQ-ENGINE-004. Size: L

## Phase 5: Commands

- [x] T-12 **Management commands** — `src/commands/list.ts` (lists workflows with triggers, inputs, descriptions), `src/commands/validate.ts` (checks YAML syntax, required fields, `${{ }}` refs valid + precede, agents exist, no duplicate triggers, input refs defined), `src/commands/dry-run.ts` (simulates execution: resolve inputs, interpolate known values, mark runtime-dependent as `<pending>`, show step sequence), `src/commands/router.ts` (routes `/w7s <sub>` to handler). Deps: T-06, T-05, T-02. Tests (mock registry): list shows all workflows; validate passes clean workflow, detects broken ref, detects missing agent, detects duplicate triggers; dry-run interpolates known inputs, marks `<pending>` for step outputs. Files: `src/commands/list.ts`, `src/commands/validate.ts`, `src/commands/dry-run.ts`, `src/commands/router.ts`, `tests/commands/commands.test.ts`. Reqs: REQ-CMD-LIST-001, REQ-CMD-VALIDATE-001, REQ-CMD-DRYRUN-001. Size: M

## Phase 6: Logging

- [x] T-13 **Execution logger + rotation** — `src/logging/logger.ts` (`ExecutionLogger`: creates JSONL log file, `logStep(stepResult)` appends line `{ts, stepId, event, data}`, `finalize(workflowResult)` writes summary + closes), `src/logging/rotation.ts` (`rotateRuns(workflowName, maxRuns, dir)` deletes oldest when count > limit). Deps: T-02. Tests: log file created in correct path, step events appended as JSONL, finalize writes summary line, rotation keeps exactly N logs and deletes oldest, rotation with fewer than N logs is no-op. Files: `src/logging/logger.ts`, `src/logging/rotation.ts`, `tests/logging/logging.test.ts`. Reqs: REQ-LOG-001, REQ-LOG-002. Size: S

## Phase 7: Integration — Wire Plugin

- [x] T-14 **Plugin integration** — Wire everything in `src/index.ts`: on init call `WorkflowLoader.load()`, register `command.execute.before` hook to match triggers → parse inputs → run engine → log result, register `/w7s` command routing to commands, format output parts for command response. Deps: T-01, T-06, T-07, T-11, T-12, T-13. Tests (mock ctx): plugin loads workflows on init, trigger command intercepted and runs workflow, `/w7s list` routes correctly, `/w7s validate` routes correctly, unknown trigger passes through, workflow result formatted as output parts. Files: `src/index.ts`, `tests/integration/plugin.test.ts`. Reqs: all (integration). Size: M
